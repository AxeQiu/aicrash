# 服务端口（可选，默认 3000）
PORT=3000

# 数据库配置（必填）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=aicrash

# CORS 允许的来源（可选，逗号分隔，默认 https://aicrash.news,https://www.aicrash.news）
# CORS_ORIGINS=https://aicrash.news,https://www.aicrash.news
